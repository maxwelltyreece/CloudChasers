import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        width: 50,
        height: 50,
        borderRadius: 25,
        margin: 10,
        position: 'absolute',
        top: 40,
        right: 0,
        justifyContent: 'center',
        alignItems: 'center',
    },
});
