// React Imports
import React from 'react';
import { View, Text } from 'react-native';

// Style Imports
import dashboardStyles from '../../styles/DashboardStyles';

const RecentMealLog = () => (
    <View style={dashboardStyles.learnMoreContainer}>
      <Text style={dashboardStyles.learnMoreTitle}>Learn More</Text>
    </View>
);

export default RecentMealLog;
